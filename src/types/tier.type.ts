export type RankInfo = {
  leaguePoints: number;
  losses: number;
  queueType: string;
  rank: string;
  tier: string;
  tierIcon: string;
  wins: number;
};

export type RankType = "soloRank" | "flexRank";
